# Atlas Hub 自律的デバッグ＆規律強化

## 1. JSクラッシュの自己診断
- [x] `logic.js` のソース全面読解・構文エラー特定
- [x] `index.html` の `window.onerror` ハンドラ調査
- [x] `production_logic.py` の出力形式と `logic.js` の消費形式の突き合わせ
- [x] `logic.js` の構造破損を修復（重複メソッド除去、PMオブジェクト再構成）
- [x] `index.html` の `window.onerror` 関数シグネチャ修復
- [x] `app.py` のデータインジェクション・ガードレール強化

## 2. 規律（MDファイル）の自己更新
- [x] `skill.md` に UI統合時のJSデバッグ規律を追加
- [x] `仕様書.md` を新規作成（カレンダーデータの最小定義・不純データ回避ロジック）

## 3. ガードレールの実装
- [x] `index.html` にデータ健全性チェック機能を実装
- [x] `app.py` に Python 側のデータバリデーション＆アラート追加

## 4. 検証
- [x] ブラケットバランス検証（全種一致確認）
- [x] データインジェクション動作シミュレーション確認
- [ ] ブラウザでの動作確認（ユーザーに依頼）
